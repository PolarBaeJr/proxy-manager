# Optional. Without these, the dashboard's DNS tab is disabled.
# Create a Cloudflare API token with Zone.DNS:Edit on the target zone.

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_DOMAIN=yourdomain.com
